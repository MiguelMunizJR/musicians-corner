---
interface Props {
  href: string;
  className?: string;
}

const { href, className = "" } = Astro.props;
const currentPath = Astro.url.pathname;

const samePath = href !== currentPath ? href : null;
---

<a
  class="w-[240px] h-[55px] flex justify-center items-center bg-[#26ab3c] text-white rounded-xl md:rounded-full hover:bg-[#1bc237]"
  href={samePath}
>
  Iniciar Sesión
</a>
