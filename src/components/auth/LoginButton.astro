---
interface Props {
  href: string;
  className?: string;
}

const { href, className = "" } = Astro.props;
const currentPath = Astro.url.pathname;

const samePath = href !== currentPath ? href : null;
---

<button
  class="w-[240px] h-[50px] bg-[#26ab3c] text-white rounded-full hover:bg-[#1bc237]"
>
  <a href={samePath}>Iniciar Sesión</a>
</button>
